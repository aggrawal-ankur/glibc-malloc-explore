# There are no data types, only bits, widths and interpretations.

Every programming language has data types.
  - Some are dynamically typed, where data types do exist but are not enforced strictly. Example: Python or JavaScript.
  - Other languages are statically typed, where types are "strictly enforced". Example: C

C has a bucket full of types. Some of them are really popular popular, the others are popular in niche communities.

To calculate the range of possible values, use these formulas:
```
# Unsigned Range
[0, (2^n)-1]

# Signed Range
[-(2^(n-1)), (2^(n-1))-1] 

n is the number width of the type in bits.
```

These formulas assume 2s complement representation for signed numbers, which is the standard.

Here is a complete list of data types in C:

| Data Type | Signedness | Range of Values | Format Specifier |
| :-------- | :--------- | :-------------- | :--------------- |
| char      | Implementation-defined | |
| unsigned char | unsigned | [0, (2^8)-1]
| signed char   | signed   | [-(2^7), (2^7)-1]
| |
| unsigned short, unsigned short int | unsigned | [0, (2^16)-1]
| short, short int, signed short, signed short int | signed | [-(2^15), (2^15)-1]
| |
| unsigned int | unsigned | [0, (2^32)-1]
| int, signed, signed int | signed | [-(2^31), (2^31)-1]
| |
| float |
| double |
| long double |
| long, long int, signed long, signed long int | signed |
| unsigned long, unsigned long int | unsigned |
| long long, long long int, signed long long, signed long long int | signed |
| unsigned long long, unsigned long long int | unsigned |